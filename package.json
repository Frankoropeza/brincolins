{
  "name": "brincolins",
  "type": "module",
  "version": "0.0.1",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "postbuild": "cp dist/sitemap-index.xml dist/sitemap.xml",
    "preview": "astro preview",
    "astro": "astro",
    "validate:assets": "node scripts/validate-assets.mjs",
    "validate:content": "node scripts/validate-content.mjs",
    "qa": "npm run validate:assets && npm run validate:content && npm run build",
    "check": "astro check"
  },
  "dependencies": {
    "@astrojs/sitemap": "^3.7.1",
    "astro": "^6.4.8",
    "gray-matter": "^4.0.3"
  },
  "devDependencies": {
    "@astrojs/check": "^0.9.9",
    "typescript": "^5.9.3"
  }
}
